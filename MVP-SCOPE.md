# MyHappinessClub — MVP Scope

**Decided:** 2026-07-02 (grill session)
**Status:** scope agreed — pending DB re-model (step 2) and stack rebuild (step 3)

---

## What the MVP is

A **walking skeleton of the full platform**: every user role has real, working
views over a real data model, so the whole concept is demoable end-to-end. We
build **only what is free** right now. Every capability that costs money or needs
a third-party account is left as a **clearly-marked seam** — a stub behind an
interface — so it can be plugged in later without reshaping the app.

**Three guiding rules:**
1. Every role's core views work on real data.
2. If it costs money or needs an external paid account, it's a seam, not a build.
3. The data model + directory layout must survive the later integrations without a rewrite.

> **Glossary:** domain terms (BaseItem, ShopItem, Design, DesignerCall, Bid,
> Collab, Order, …) are defined in [CONTEXT.md](CONTEXT.md).

---

## Roles (4, multi-role accounts)

One account can hold several roles at once. Everyone is a **Buyer** by default.

| Role | In MVP | Notes |
|---|---|---|
| **Buyer** | ✅ | Default. Browse, cart, checkout, order history. |
| **Seller** | ✅ | Storefront, products, design tool, hiring, analytics. |
| **Designer** | ✅ | Portfolio, browse calls, bid, collaborate, chat. |
| **PrintShop** | ✅ | Owns blank catalog + production queue. |
| **Admin** | ❌ later | Managed out-of-band (DB tools) for now. |

---

## Core commerce model

Print-on-demand **concept**: a Seller picks a **BaseItem** (blank) from a
PrintShop's catalog, applies a **Design**, sets a price → publishes a **ShopItem**.
A Buyer buys it → an **Order** flows to the PrintShop's production queue → the
PrintShop makes it and hands it to shipment.

---

## What's IN the MVP (build now, free)

### Seller
- Onboarding: pick a **Plan** (Free/Creator/Pro — recorded, billing stubbed), set up storefront.
- **Storefront editor**: cover, avatar, bio, social links (plain URLs), product order, live/draft toggle.
- **My Products**: listed / unlisted / pending sections; list & unlist; edit price/name/description/photos.
- **"Design now" tool**: upload a PNG → drag onto the blank's print area (store x/y/scale) → CSS-overlay preview. Set name/price/description, see estimated earnings. Publish as listed or unlisted.
- **"Hire a designer"**: post a **DesignerCall** (brief, reference image, deadline, payment type = fixed/%/both, optional budget).
- **Bids inbox**: review Designer **Bids**, chat, accept one → creates a Pending **Collab**.
- **Collab (seller side)**: walk the state machine, accept/decline submissions with feedback.
- **Basic analytics**: revenue over time, orders per product, top products.

### Designer
- **Portfolio** page (same shape as a storefront).
- Browse open **DesignerCalls**, submit **Bids** (price + message), manage/withdraw them.
- **Collab (designer side)**: submit work, see feedback, advance states. (Can never list the final product themselves.)
- **Chat** with the Seller (basic polled thread).
- Basic analytics (own collabs/earnings).

### Buyer
- **Landing / discovery**: categories, best-sellers, top-influencer cards.
- **All products** page with filters + basic name search.
- Product page → **Cart** (single seller, multi-item) → **Checkout** (creates a real paid Order, no real gateway).
- **Order history**.

### PrintShop
- **Catalog management**: CRUD **BaseItems** + sizes/colors/print options/base prices.
- **Production queue**: paid Orders to make → mark `in_production` → `handed_to_shipment`.

### Cross-cutting (IN)
- **Auth** + multi-role accounts + role-based routing/guards.
- **In-app Notifications**: bell + list (sale, new bid, bid accepted, collab state change, new production order). Email delivery = seam.
- **Plans**: 3 tiers as config; free limits enforced (product cap, AI quota); paid perks shown but gated.
- **AI credits**: balance shown, decrements on (stubbed) AI generation.
- **Orders + plan-based Commission**: `Free 15% / Creator 10% / Pro 5%`, computed & stored at order time.

---

## Seams — left open for later paid/API integration

Each is an interface with a stub implementation now; swap in the real service later.

| # | Seam | MVP stub behavior |
|---|---|---|
| 1 | **GenAI image generation** | "Generate with AI" button returns a placeholder / mock image. |
| 2 | **Mockup rendering** | Preview = CSS overlay of art on the blank photo (no realistic render). |
| 3 | **Payment gateway** (Stripe) | Checkout marks Order `paid` with no charge; subscription billing + designer fixed-fee **escrow** stubbed. |
| 4 | **Cargo / shipment** | Ends at `handed_to_shipment`; tracking + cargo notifications stubbed. |
| 5 | **Social OAuth + follower verification** | Social links are plain URLs; no verified follower counts. |
| 6 | **Email / push delivery** | Notifications are in-app only. |
| 7 | **File / image storage** | Uploads to local/free storage; cloud (S3/etc.) later. |
| 8 | **Search** | Basic name match; AI/vector search later. |

---

## Explicitly DEFERRED to later phases

- **Admin** dashboard (approvals, moderation).
- **Follower-based commission** brackets (`FollowersPercentage`) + **follower-gated products** (`only_followers`).
- **Cargo/delivery modeling**: pickup/drop options, cities, delivery times, base price.
- **Promotions**: discount codes, referral links, affiliate tracking (shown as locked plan perks).
- **Real-time chat** (websockets) — MVP chat is a polled message thread.
- **Custom subdomain** + white-label (Pro perk).
- **AI add-on credit packs**, high-res/commercial AI tiers.
- **Advanced analytics**: sales-by-country, calendar.
- **Real payments, real AI, real mockups, real fulfillment** — see seams above.

---

## Next steps (per project plan)

1. ✅ **Step 1 — MVP scope agreed** (this doc).
2. ✅ **Step 2 — Database re-modelled** → [DATA-MODEL.md](DATA-MODEL.md) (added Order/Cart/production-queue; cleaned User/Role/Design/Collab).
3. ✅ **Step 3 — Stack scaffolded** → FastAPI + SQLAlchemy/Alembic backend (`/backend`) + Vite/React SPA (`/frontend`) + Docker Postgres. 27 tables mapped; auth + catalog live; every other role surface stubbed. Old Next.js build archived in `/legacy`. See [docs/adr/0001](docs/adr/0001-stack-fastapi-react-postgres.md).
4. 🚧 **In progress — Fill in the role surfaces.** Implement the stub routers + build the React views per role, following the `catalog` router pattern and swapping seams as paid services come online.
   - ✅ Seller **auth** (JWT, Google, reset, verify seam) + **onboarding** wizard.
   - ✅ Seller **storefront**: brand editor (logo/cover upload → local-disk storage seam #7 with Pillow optimize, description, social links, custom store URL), in-app Preview, Draft→Publish, and the public `/store/{slug}` page (product grid stubbed until products ship).
   - ✅ Seller **dashboard** (`/dashboard`, role-aware): welcome + onboarding checklist (derived from real state), quick actions, and **in-app notifications** — real router (list/unread/mark-seen/mark-all), ~45s polling (websockets = deferred seam #6), Welcome+Tip seeded at signup, top-bar bell + profile dropdowns.
   - ✅ Seller **catalog browse** (`/seller/catalog` + `/catalog/:id`): enriched public catalog API (search, dynamic facet filters — category/provider/color/material/method/price/availability, sort, offset pagination), rich cards (image/colors/areas/provider), product detail, **favorites** (seller-scoped table + toggle), recently-viewed (localStorage). Demo catalog seeded via `python -m app.seed_catalog` (2 partners × ~13 blanks, Pillow placeholder images). 'Synced from print providers' remains a seam.
   - ✅ **Product configurator** (the `/catalog/:id` page, upgraded): interactive color/size/material/print-type/position selectors with **live per-unit price** (base + variant + option deltas), variant-aware availability, production time + shipping note, single-image preview + colour indicator (per-colour mockups = deferred mockup seam #2). Two CTAs (Design yourself / Hire a designer) carry the chosen `ProductConfig` forward via route state. Detail API returns structured `print_options` (w/ deltas) + `production_time`.
   - ✅ **Design editor** (`/seller/design/:baseItemId`): CSS-overlay canvas — drag-move / resize / rotate / center / remove / replace on the blank, print-area frame (Front/Back/…), colour indicator. **Upload** (PNG/JPG/WebP transparency-preserving + SVG raw via `save_design_image`; recent-uploads reuse) and **AI generate** (seam #1 — `POST /designs/generate` returns 4 Pillow-stubbed candidates, real AI-credit ledger: 10 granted once, −1/gen, 402 when empty). Client-side warnings (low-res / print-area exceeded / missing transparency), undo/redo history, localStorage autosave. 'Continue' carries `{design, placement, config}` to the product-info step. Realistic mockups stay mockup seam #2.
   - ✅ **Product information / publish** (`/seller/products/new` + real `shop_items` router): finalize name/description/category/tags/price, **AI copy** (free templated title/description/keywords, seam #1), **profit calculator** (base cost · plan commission fee · earnings · retail price, live; break-even minimum enforced client+server), review summary + validations, **Save Draft / Publish** → transactional `ShopItem` create (LISTED/UNLISTED). **My Products** lists real items (Listed/Drafts/Pending, list/unlist/delete). Published products now render on the **public storefront** (`/store/:slug`) as design-on-blank composites. Post-publish mockup-gen (seam #2) + search indexing (seam #8) are logged no-ops.

   - ✅ **Hire a designer / job offers** (`/seller/hiring/*` + designer marketplace): `DesignerCall` extended (base product, design style, inspiration notes, revenue-share %, desired launch date, `DRAFT` status) plus a `CallAttachment` table (reference images via the storage seam + brand-guideline PDF passthrough). Create/edit form with compensation (**Fixed / Revenue share / Both** — payment-type-driven required fields, 1–50% cap), timeline (no past deadlines), attachments, **Save draft / Publish**. **My Job Offers** list (publish/unpublish/edit/delete; editable until awarded) and a read-only **Designer Marketplace** (`/designer` list + `/designer/calls/:id` detail) showing only OPEN jobs. Bidding/applications = next goal.

   - ✅ **Hiring hub + applications** (`/seller/hiring` + `/seller/hiring/:id`): the seller's central job-management page — **7-state status derived** server-side from call + bids + collaboration (Draft → Open → In review → Designer selected → In progress → Completed → Closed, so it can never drift), application counts, publish date, deadline, and actions **View / Edit / Close / Duplicate / Delete-draft-only**. **Real bidding:** designers apply from the marketplace (price / % / message, revisable); the seller reviews applications and **Accepts** one → winning bid ACCEPTED, others REJECTED, call AWARDED, `Collaboration` opened (PENDING). In-app notifications on new application + acceptance. `POST /designer/enable` ("Become a designer") + 3 seeded demo designers (`python -m app.seed_designers`, password `Password123`) make both sides testable. Collaboration workspace (submissions/chat) = next goal.

   - ✅ **Applicant review**: applicant cards now carry real social proof — **ratings & reviews** (new `DesignerReview` table; seeded for demo designers), **portfolio preview** (their `Design` artwork), **completed jobs**, and a **derived response time** (median publish→apply latency, no fabricated column). Public **designer profile** at `/designers/:slug` (bio, rating, reviews, portfolio grid). Explicit **Reject** per applicant (final + notified, job stays open) and accepting now notifies the auto-rejected designers too. Chat button stubbed until the collaboration workspace.

   - ✅ **Collaboration workspace** (`/collaborations/:id`, shared by both parties): project overview (product, brief, deadline, agreed terms), **append-only `CollabEvent` timeline** (started → draft → revision → approved → paid → product created) with the current stage **derived** from state + events, submissions with approve / request-revision (feedback preserved), designer draft & final uploads, and a **polled message thread** (`ChatMessage`) — both sides notified on every step. **Payment** appears only for fixed-fee deals (payments seam #3 stubbed); percent-only deals skip it. On approval a **draft `ShopItem` is auto-created** (UNLISTED, price pre-filled to break-even via the shared `core/pricing` rule) and linked to the collaboration, landing in My Products ready to price & publish. Designer list at `/designer/collabs`; sellers jump in from an awarded job.

   - ✅ **Website builder** (`/seller/storefront`, replaces the earlier storefront editor): introduces a **draft layer** — a `storefront_draft` JSON holds pending edits while the existing columns stay the published truth, so *editing never touches the live page until Publish* (this **reverses** the earlier single-record live-edit decision). Panels for branding (logo/cover with recommended dimensions, name, description), **theme** (preset + custom hex primary/accent, button style — applied via CSS variables), store info (URL, contact email, location), 6 social links (now incl. **X**), and **homepage curation** (reorder / feature / hide, stored in the config so it rides the same draft/publish flow). **Debounced autosave**, Discard changes, and a **live preview with Desktop/Mobile toggle** rendering the real `StorefrontView`. Public `/store/:slug` serves published values only, applying theme + curation (featured first, hidden excluded).

   - ✅ **Publishing & public URL** (same page): three statuses — **Draft** (never published) / **Published** / **Unpublished** (was live, taken down) — **derived** from `store_state` + `published_at`, never stored separately. Shown prominently in a status card with the live URL (copy + visit), last-published time, and a blocker list that gates the Publish button. **Store URL rules**: normalized as typed, 3–32 chars, reserved words blocked, live availability check with a free-slug suggestion; publish **refuses a taken slug** rather than silently handing out a different one, and warns that moving a live store breaks old links. Full-page **Preview** (`/seller/storefront/preview`) renders the draft through the *same* `core/storefront_render` helper the public page uses, so preview can't drift from what ships. **Cache**: public `/store/{slug}` sends `ETag: W/"slug-vN"` + `Cache-Control: max-age=60, must-revalidate`; every publish/unpublish bumps `storefront_version`, so a republish invalidates caches immediately. Unpublishing 404s the public page but keeps all content for a one-click republish.

   - ✅ **My Products** (`/seller/products`): four tabs — **Drafts / Published / Collaborations / Archived**. Adds `ARCHIVED` to `ShopItemState` (a safe retirement that isn't Delete: hidden from the storefront, restorable) and `updated_at` to `ShopItem` for the card's "last updated". State moves go through a **transition table** (`ARCHIVED → LISTED` is refused; restore always lands in draft), and **deleting a LISTED product 409s** — unpublish or archive first. **Duplicate** copies design, placement and price into a fresh draft. The **Collaborations tab reads from `Collaboration` rows, not products** — no ShopItem exists until the designer's work is approved — showing derived stage, designer and deadline with a link to the workspace. `GET /shop-items` now returns `{items, counts, categories}` with **server-side** `q` / `state` / `category` / `created_from` / `created_to` / `sort`; **counts and category options are computed over the whole catalogue** so tab badges stay honest while a filter narrows the grid. **Edit** at `/seller/products/:id/edit` — name, description, tags, price with the live profit breakdown, plus Save & publish / unpublish / archive / duplicate.

   - ✅ **Product editor** (`/seller/products/:id/edit`): edits every facet of an existing product — information (name, description, tags; category stays read-only since it comes from the blank), **variant + print options**, **design**, and visibility. `ShopItem` now **persists `color`/`size`/`material`/`print_type`** (previously only the derived `production_cost` was kept, so the chosen variant was unrecoverable); existing rows were backfilled from their blank's first available combo. Changing a variant **re-derives `production_cost` server-side** and validates the combo against the print partner's stock; if the new cost outgrows the price the save is **refused with the new minimum** rather than silently repricing. **Design editing reuses the creation-flow design editor** (`/seller/design/:baseItemId` opened with an `editing` payload, returning the chosen artwork + placement to the edit page unsaved) instead of a second canvas that would drift — and it skips the localStorage autosave so it can't clobber a half-built new product. Artwork or placement changes **regenerate mockups** (seam #2). Every save writes an append-only **`ShopItemRevision`** with human-readable lines ("Price €29.00 → €34.00", "Design replaced"), surfaced as **Change history** on the page. Historical orders were already safe — `OrderItem.unit_price_at_purchase` snapshots the price at purchase.

   - ✅ **Orders** (`/seller/orders` + `/seller/orders/:id`): the full commerce loop. **Buy-now checkout on the public storefront** (variant + quantity + shipping form → stubbed payment, seam #3) creates real orders — buyers become lightweight passwordless users (find-or-create by email). **Status model:** money facts on `Order` (PENDING/PAID/CANCELLED/+**REFUNDED**), production facts per line (`FulfillmentStatus` +**SHIPPED**/**DELIVERED**), and the ticket's **7 display statuses derived** from both; an append-only **`OrderEvent` timeline** records placed→paid→production→shipped→delivered/cancelled/refunded. Order cards carry short ID, customer, product summary, date, total, **earnings** (commission snapshotted at purchase); server-side **search** (customer/order #/product) + status tabs with whole-set counts + date + revenue sort. Detail shows customer, lines with **variant snapshot** (`name_at_purchase` — renames can't rewrite history), shipping address, **tracking number**, stub payment info, timeline, and guarded **Cancel** (only before production) / **Refund** (paid only). **The print shop drives fulfillment**: real `/production/queue` page (paid items per shop, advance Paid→In production→Shipped(+tracking)→Delivered); each aggregate transition auto-logs the event and **notifies the seller** — the ticket's "orders update automatically when the provider updates" made real. Public storefront products now include in-stock variants for the buy dialog.

   - ✅ **Order detail deepening** (`/seller/orders/:id`): full transparency per order. **7-step fulfillment stepper** (Order received → Payment confirmed → Sent to print provider → Production started → Quality check → Shipped → Delivered) rendered from the event log — done/current/upcoming with timestamps; cancelled/refunded orders show the factual log only. **Quality check is a real queue step** for the print shop (Paid → In production → QC → Shipped(+tracking) → Delivered); the 7 seller statuses stay stable (QC displays as In production). "Sent to provider" logs automatically at payment, named after the shop. **Money fix:** order payout previously ignored production cost, contradicting the product calculator — now each line snapshots `production_cost_at_purchase` (+ design placement, so the mockup shown is what the buyer bought and later product edits can't rewrite the order) and **earnings = subtotal − production − commission**; existing orders recomputed in the migration. Detail shows per-line unit price/cost/line totals and the order-level breakdown.

   - ✅ **Analytics** (`/seller/analytics`): overview cards (Revenue / Orders / **Visitors** / **Conversion** / AOV) + Recharts charts (revenue area, orders + visitors small-multiple lines — one axis each, never dual-axis), horizontal bars for **sales by country**, **top products**, **best categories**, and a **product performance table** (revenue, units, conversion, rating — rating shows "—" until buyer reviews exist). **Visitors are real**: new `StoreVisit` table counts one salted-IP-hash row per storefront per day (no cookies, `ON CONFLICT DO NOTHING`), recorded on the public page; conversion = orders ÷ unique visitors. Revenue counts PAID orders only (cancelled/refunded excluded); one `/seller/analytics?date_from&date_to` endpoint computes totals + zero-filled per-day series + breakdowns per request (no stats tables to drift). Date presets Today/7/30/90 + custom range; auto-refresh via a 60s poll. Chart palette validated with the dataviz six-checks (#6366f1/#0ea5e9 on light, pass).

   - ✅ **Notifications center** (`/notifications`, linked from the bell's "View all"): full page with **date grouping** (Today / Yesterday / This week / This month / Earlier) and **category filters** (Sales, Orders, Payments, Applications, Collaborations, Announcements, System). Enum gained **ORDER / PAYMENT / ANNOUNCEMENT / REVIEW** (REVIEW reserved for buyer reviews); order-progress notifications reclassified from SALE→ORDER, refunds now push a PAYMENT notification ("Refund issued · €X"). **Actions:** mark as read, mark all as read, **delete** (owner-checked, 404 for others), open related page via `link_url`. **Email mirroring**: `notify.push()` now also sends through the delivery seam (🔌 stub logs instead of sending) with an `email=False` opt-out used for low-stakes items like the welcome pair — in-app + email both supported, noise kept down.

   - ✅ **Account settings** (`/settings`, from the profile menu): **Personal** (names with display-name follow, profile photo upload/remove, email with verified badge), **Business** (VAT/tax ID — new `tax_id` field, country, currency; brand name shown read-only with a link to the website builder — editing it here would bypass the draft/publish contract), **Store** (URL + publish state, read-only + builder link — same reasoning), **Preferences** (language, timezone — new `Settings.timezone` — plus email-notifications and marketing toggles; take effect immediately), **Security**: change/set password (current password required; Google-only accounts can set one), change email (password-confirmed + re-verification link via the email stub; 409 on taken; blocked for Google-managed accounts), connected accounts, and **active sessions with a real "log out of all other devices"** — tokens now carry `iat` and a `sessions_revoked_at` cut on the user kills every earlier token (floored to whole seconds, since JWT iat is; the replacement token survives). Password changes also revoke all other sessions.

   - ✅ **Buyer accounts** (`/signup` buyer-first, `/sell` for creators): splits signup into a **buyer-only** path (a pure Buyer who can start a store later) and the existing seller path, so shopping no longer drags people through storefront onboarding. **Guest-claim**: checking out as a guest creates a passwordless Buyer keyed by email; signing up (or Google sign-in) with that same email **adopts that account and its prior orders** instead of 409-ing — a real account (password/OAuth) is still a hard conflict. **Terms & Privacy acceptance** is required on every signup path (`users.accepted_terms_at`; Google implies consent), with stand-in `/terms` + `/privacy` pages. Email lookups are now **case-insensitive** (login/register/forgot) so mixed-case and lowercased-guest emails resolve to one account. **Remember-me, reset, verify, logout-everywhere** were already built and now serve buyers too. **Return-to redirect**: the auth guard remembers the page it bounced you from and public "log in" links carry `?next=`, so after auth you land back where you were (buyers → the storefront, returning users → dashboard), open-redirects blocked. **Buyer dashboard** (`/orders` + `/orders/:id`): real order history across all storefronts — status badge, thumbnail, tracking, total — and a detail page with the same **7-step fulfillment stepper**, tracking banner, item mockups (purchased placement), factual timeline, shipping address and a link back to the store; seller economics (commission/production/earnings) are omitted. A **logged-in checkout attaches the order to the account** (not the typed email); the buy dialog pre-fills the buyer's details and, for guests, the success screen **nudges them to create an account** to keep the order. **Apple Sign In** is deferred (Google OAuth is the live provider; seam #5-adjacent).

> **Seller MVP is complete — the happy path runs end-to-end:** signup → onboarding → dashboard → browse catalog → configure → design (upload/AI) *or* hire a designer → collaborate to an approved design → set price & publish → customize the storefront → live.
>
   - ✅ **Buyer account / profile** (`/settings`, now role-aware): the account page **adapts to roles** — everyone gets Personal + **Saved addresses** + Preferences + Security; only sellers additionally see Business + Store (a pure buyer never meets an empty seller section). **Personal** gained an optional **phone** (`users.phone`). **Preferences** gained a **currency** picker (`settings.currency`, a stored display preference — prices stay € until an FX layer lands) alongside language, timezone and the email/marketing toggles (all save instantly). **Saved addresses** are the new capability: a new `addresses` table + `AddressType` enum (SHIPPING / BILLING), **one default per type** (promoting one demotes its sibling, enforced server-side), full CRUD (add / edit / delete / set-default), a **"use for both"** toggle that creates a billing twin of a shipping address, and ownership-scoped access (another user's address 404s). **Checkout uses them**: a logged-in buyer's **default shipping address pre-fills** the buy-now form, with a dropdown to pick any saved shipping address — closing the loop the feature exists for. Security (password/email confirmation, connected accounts, active sessions, log-out-everywhere) is the same hardened flow the seller settings already had. Payment methods stay deferred (checkout is the stubbed gateway, seam #3).

   - ✅ **Marketplace homepage** (`/`, now buyer-first — the seller pitch moved to `/sell`): a discovery hub aggregating **across every live storefront** via one public `GET /marketplace/home` payload — a **hero** (headline/CTA + a featured brand card + hero product), **Shop by category** tiles, **Featured products**, **Trending brands**, and **New arrivals**. With no Admin Panel yet, every section is **derived from real signals**: featured = best-selling (PAID order count), trending brands = most sales in the last 30 days then recent visits, new arrivals = most recently listed — each with a **newest-first fallback** so a fresh platform never shows an empty shelf (admin curation is a documented future override). **Categories are data-backed** (`ItemCategory` rows that actually have live products, with counts + a representative mockup + mapped icons). A **`/marketplace` listing page** does category browse (chips + `?category=`) + name search (debounced into the URL) + sort (popular/newest/price) + "load more" paging via `GET /marketplace/products`. Product cards render the design-on-blank mockup and **open the creator's storefront** (`/store/:slug`, where buy-now already lives) — no second checkout surface. Only LISTED products in LIVE stores are ever shown; images lazy-load.

   - ✅ **Marketplace search & filters** (`/marketplace`, extends the listing): a familiar e-commerce browse rail. **Search spans product name + brand name + keywords** (the seller's `tags`) in one query. **Filters**: category, **price** (min/max), **brand** (multi-select, from a facet list with product counts), **color** and **size** — the last two matched against the **blank's buyable variants** (a product offered in red surfaces under "Red" even if the seller's primary pick was blue), not just the stored primary. **Sort**: most popular (best-selling) / newest / price ↑ / price ↓. A new **`GET /marketplace/facets`** returns all filter options over the live catalogue (categories, brands+counts, colors, ordered sizes, price range) — static, not per-selection counts (dynamic faceting is a future nicety). All filter state lives in the **URL** (shareable, back-button-friendly) and drives a debounced React-Query fetch, so results **update dynamically without a reload**; a left rail on desktop collapses into a mobile drawer, "Load more" paginates, and images lazy-load. Cards still open the creator's storefront (a dedicated Product Details Page is its own next ticket).

   - ✅ **Brand storefront shopping** (`/store/:slug`, extends the public page): the creator's shop now has a real **shopping experience** on top of the existing brand header (logo, cover, name, description, socials incl. **Website**, location, contact). A distinct **Featured** row shows the products the seller curated as featured, then an **All products** grid with **sort** (Featured / Price ↑ / Price ↓) and **filters** — category chips + colour swatches (from the products' buyable variants). Everything is **client-side over the single storefront payload** (a brand's catalog is bounded — no new endpoint, instant), and the Featured row **hides the moment a buyer narrows** (curation is a default, not a constraint). The public product payload gained a `category` field to power this. Filtering/sorting is gated to the public page via a `shop` prop, so the builder's live preview (which is about editing, not shopping) is unchanged and preview-still-equals-live for theme/content. Fully responsive (existing mobile layout); "open product" opens the buy-now dialog (quick-view/PDP stays a future item).

   - ✅ **Product Details Page + Cart** (`/p/:id` + `/cart`): the dedicated PDP the earlier tickets deferred. **Gallery** — the design-on-blank composite as hero with click-to-zoom, plus thumbnails for the composite / artwork-alone / blank-alone (real multi-angle & lifestyle shots stay mockup seam #2). **Info** — name, brand link, price, description, category, **materials + print method** (from the blank's print options), available colours/sizes. **Variant selection** — colour swatches + size chips with **unavailable combos disabled** (availability from the blank's buyable variants), quantity stepper; **validation** blocks add/buy until an in-stock colour+size is chosen. **Additional info** — estimated delivery (from production time), shipping, returns, secure-checkout notes. **Related** — same-brand products then similar-category from other stores. **Purchase**: *Buy Now* reuses the existing single-item checkout (pre-filled with the chosen variant); *Add to Cart* feeds a **localStorage cart** (works for guests + logged-in, syncs across tabs). The **cart page groups lines by brand** (each creator ships from its own print partner) and checks out each brand as **one multi-line order** via a new `POST /store/{slug}/checkout-cart` (validates every line's stock before the stubbed payment, then one Order with N OrderItems). A **cart badge** rides the public + storefront headers; marketplace/related cards now open the PDP. New public read: `GET /marketplace/products/{id}`. Payment methods remain the stubbed gateway (seam #3); wishlist / 360°-viewer / reviews stay future items.

   - ✅ **Shopping cart & order summary** (`/cart`, extends the cart): a full review-before-checkout experience. Each line shows image, name, brand, variant, quantity stepper, **unit price** and line total, with remove + **Continue shopping**. A per-brand **Order Summary** breaks down **Subtotal − Discount + Shipping + Tax = Total** (recomputed live on every change), and **discount codes** are real: a new `DiscountCode` table (percent or fixed, active flag, optional min-subtotal/expiry, optional per-brand scope, usage counter) with a `POST /marketplace/discount/validate` endpoint and application at checkout (seeded demos **WELCOME10** = 10% off, **SAVE5** = €5 off over €25). Shipping is a flat €4.99 **waived over €60** (config-driven; real cargo pricing stays seam #4); tax is a configurable rate (0 by default). **Money model:** the order now stores `discount_amount` / `shipping_amount` / `tax_amount` / `total_amount` + the code; the stub charges the **total**, but **seller payout is unchanged** (subtotal − commission − production) — shipping/tax aren't seller revenue and the promo is **platform-absorbed**. The buyer dashboard's list + detail now show the amount actually paid with the full breakdown. **Persistence** matches the ticket: guests keep a localStorage cart; a **logged-in buyer's cart lives on the server** (the existing Cart/CartItem tables, per-brand) and **merges the guest cart on login**, then follows them across devices — the cart is self-cleaning (lines whose product unlisted or variant sold out are dropped on read/write). New endpoints: `GET/PUT/POST /cart`. Validations: unavailable variant blocks checkout (whole cart, no partial charge), empty cart can't check out, quantities capped (numeric per-variant inventory isn't modelled — availability is boolean).

   - ✅ **Checkout & order confirmation** (`/checkout/:slug` + `/order/confirmed/:token`): a dedicated one-page checkout replacing the modal. Sections: **Contact** (email + optional phone), **Shipping address** (full name, street, **apartment**, city, postal, country + saved-address picker/prefill for logged-in buyers), **Shipping method** (**Standard** €4.99/5–7 days, free over €60, vs **Express** €12.99/2–3 days — each with delivery estimate + cost, driving the total), **Payment** (card fields with **PCI-safe practice — card data is validated client-side (Luhn/expiry/CVC) and never sent to our server**; Apple/Google Pay + PayPal shown as "soon"; secure-checkout trust bar), and a sticky **Order Review** summary the buyer confirms before paying. **Duplicate-order protection**: a client-generated **idempotency key** per attempt — a repeat submit (double-click/retry/back-button) returns the *same* order server-side instead of creating a second; the button also disables while in flight. **Inventory re-checked** before the (stubbed) authorization; the order is created and the seller + print provider notified only on success. **Order confirmation**: the checkout returns a **signed confirmation token**; a public `GET /marketplace/order-confirmation?token=` renders a **guest-safe, refreshable** confirmation page (thank-you, order #, items, shipping method + estimate, address, full money breakdown) — no order is reachable by guessing an id — and a **confirmation email** goes out via the delivery seam. New: order columns `idempotency_key` / `shipping_method` / `ship_phone`; `GET /marketplace/shipping-methods`; confirm-type JWT. Payment gateway itself remains seam #3 (test authorization).

> **Buyer path runs end-to-end too:** land on the marketplace homepage → browse / search / filter → open a **product details page** (gallery, variants, related) → Add to Cart across brands (server-persisted when logged in) *or* Buy Now → review cart with discount code + order summary → **one-page checkout** (contact, shipping, method, payment, review; saved-address prefill; duplicate-safe) → **order confirmation page + email** → sign up (claims the order) → order history with full money breakdown + live tracking in the buyer dashboard.
